# Neural Networks — Assignment

| Directory | Problem set | Task |
|---|---|---|
| [`problem-set-01-pneumonia-cnn/`](problem-set-01-pneumonia-cnn) | 01 | CNN classifier for paediatric chest X-rays, NORMAL vs PNEUMONIA, 5,856 images |
| [`problem-set-02/`](problem-set-02) | 02 | — |

Each directory is self-contained: source code, `requirements.txt`, and a `README.md`
covering the approach, methodology and findings.

Datasets are not committed to this repository. They are large and publicly available;
each problem set's README states where to obtain the data and where to place it.

## Setup

```bash
cd problem-set-01-pneumonia-cnn
pip install -r requirements.txt
```
