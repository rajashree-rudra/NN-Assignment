# Unpinned on purpose: the wheel available depends on your Python version.
# Verified against TensorFlow 2.16-2.21 (all ship Keras 3, same API).
tensorflow>=2.16
scikit-learn>=1.3
matplotlib>=3.7
numpy>=1.24
pillow>=10.0
jupyter>=1.0
